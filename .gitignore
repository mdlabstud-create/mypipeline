node_modules
dist
coverage
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/*.log
playwright-report

# Generated exports (mounted from Docker at src/generated/)
src/generated/

# Environment files: ignore everything except the committed template.
.env
.env.*
!.env.example

# Local-only secret quarantine (created by secrets-hygiene cleanup)
.secret-backups/

# Bundled binaries that don't belong in the repo
ngrok.exe
*.exe
